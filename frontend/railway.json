{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS_NODE_BUILDER_STATIC",
    "buildCommand": "npm install && npm run build",
    "outputDir": "dist"
  },
  "deploy": {
    "staticFiles": {
      "path": "dist",
      "singlePageApp": true
    }
  }
}